# CLAUDE CODE BRIEF — TTS SHOP
## Переписать фронтенд с нуля по HTML-макетам и ТЗ
**Дата:** 22 февраля 2026

---

## ЦЕЛЬ

Переписать React-приложение TTS Shop с нуля, точно воспроизводя дизайн из 13 HTML-макетов. Подключить к существующему Supabase-бэкенду. Все таблицы в БД на русском языке.

---

## ТЕХ-СТЕК

- **React 18 + Vite + Tailwind CSS** (сохраняем)
- **Zustand** — стейт-менеджер (заменить самописный AppContext)
- **React Router v6** — роутинг
- **Supabase JS** — клиент БД
- **Шрифт:** Cinzel (Google Fonts)
- **Деплой:** Vercel + Vercel Serverless Functions (/api)

---

## ДИЗАЙН-СИСТЕМА (извлечена из макетов)

### CSS-переменные (одинаковые во всех 13 макетах):
```css
:root {
  --gold-light: #f4e4a6;
  --gold: #d4af37;
  --gold-dark: #b8860b;
  --gold-darker: #8b6914;
  --bg-dark: #0a0908;
  --bg-card: rgba(20, 18, 15, 0.95);
  --border-gold: rgba(212, 175, 55, 0.3);
  --text-muted: rgba(212, 175, 55, 0.6);
  --green: #4ade80;
  --yellow: #fbbf24;
  --red: #f87171;
  --blue: #60a5fa;
}
```

### Типографика:
- Шрифт: `'Cinzel', serif`
- Заголовки: font-weight 700-900, letter-spacing 1.5-4px, CAPS
- Тексты: font-size 10-14px
- Мутированный текст: color var(--text-muted)
- Золотой градиентный текст для заголовков:
  ```css
  background: linear-gradient(180deg, var(--gold-light) 0%, var(--gold) 50%, var(--gold-dark) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  ```

### Контейнер приложения:
- max-width: 430px, margin: 0 auto
- Фон: кожаная текстура (`assets/leather-texture.png`) + radial-gradient overlay
- Все карточки: bg-card, border 1px solid border-gold, border-radius 12-20px

### Кнопки:
- Золотые (primary): `background: linear-gradient(145deg, var(--gold), var(--gold-dark))`, color: bg-dark
- Карточные: bg-card с border-gold
- Active scale: `transform: scale(0.95)` при нажатии

### Статусы:
- 🟢 green (#4ade80) — в наличии, выполнен
- 🟡 yellow (#fbbf24) — мало, собирается
- 🔴 red (#f87171) — нет, отменён
- 🔵 blue (#60a5fa) — в пути

### Нижняя навигация (BottomNav):
По ТЗ должна быть 5-кнопочная с центральной выступающей кнопкой "Быстрый заказ":
```
[Главная] [Каталог] [⚡БЫСТРЫЙ ЗАКАЗ⚡] [Избранное] [Профиль]
```
- Центральная кнопка: 60x56px, круглая, золотой градиент, выступает вверх, box-shadow
- Остальные: SVG иконки 26px
- Активный пункт: золотой цвет, более яркая иконка

**ПРИМЕЧАНИЕ:** В HTML-макетах навигация 4-кнопочная (без центральной). Реализовать по ТЗ — 5 кнопок.

---

## СТРУКТУРА ФАЙЛОВ (целевая)

```
tts-client-app/
├── api/                          # Vercel Serverless Functions
│   ├── send-sms.js              # POST: отправка SMS через SMS.ru
│   ├── verify-sms.js            # POST: проверка SMS кода
│   ├── register.js              # POST: регистрация клиента
│   ├── login.js                 # POST: вход по логину/паролю
│   └── create-order.js          # POST: создание заказа (атомарная транзакция)
├── public/
│   └── assets/
│       └── leather-texture.png  # Кожаная текстура фона
├── src/
│   ├── main.jsx                 # Entry point
│   ├── App.jsx                  # Router + auth guard + layout
│   ├── index.css                # Tailwind + CSS-переменные + глобальные стили
│   │
│   ├── lib/
│   │   ├── supabase.js          # Supabase client
│   │   └── auth.js              # getSavedSession(), clearSession(), saveSession()
│   │
│   ├── stores/
│   │   ├── authStore.js         # Zustand: клиент, авторизация, сессия
│   │   ├── cartStore.js         # Zustand: корзина, расчёт скидок
│   │   └── filtersStore.js      # Zustand: фильтры каталога
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppShell.jsx     # Обёртка: фон, max-width, padding-bottom
│   │   │   ├── BottomNav.jsx    # 5-кнопочная навигация
│   │   │   ├── Header.jsx       # Sticky header (лого / back + title + actions)
│   │   │   └── PageHeader.jsx   # Header с кнопкой назад
│   │   │
│   │   ├── product/
│   │   │   ├── ProductCard.jsx      # Карточка товара (сетка 2 в ряд)
│   │   │   ├── ProductCardSmall.jsx # Маленькая карточка (горизонтальный скролл)
│   │   │   ├── ProductPanel.jsx     # Панель товара (как в "Топ-спрос")
│   │   │   ├── StockDots.jsx        # Индикаторы наличия Ц/С/Л (3 точки)
│   │   │   └── StrengthBar.jsx      # Шкала крепости
│   │   │
│   │   ├── ui/
│   │   │   ├── GoldButton.jsx       # Золотая кнопка (primary)
│   │   │   ├── CardButton.jsx       # Кнопка-карточка
│   │   │   ├── Chip.jsx             # Чип фильтра (с точкой статуса)
│   │   │   ├── Badge.jsx            # Бейдж (ТОП, NEW, -15%)
│   │   │   ├── QuantitySelector.jsx # Счётчик +/- 
│   │   │   ├── Divider.jsx          # Золотой разделитель
│   │   │   └── SectionHeader.jsx    # Заголовок секции с "СМОТРЕТЬ ВСЕ →"
│   │   │
│   │   └── cart/
│   │       ├── CartItem.jsx         # Элемент корзины
│   │       ├── PaymentSelector.jsx  # Выбор способа оплаты
│   │       ├── PickupSelector.jsx   # Выбор точки получения
│   │       └── TcoinsInput.jsx      # Поле списания ткоинов
│   │
│   └── pages/
│       ├── Onboarding.jsx       # 4 слайда (13-onboarding.html)
│       ├── Home.jsx             # Главная (01-home.html)
│       ├── Catalog.jsx          # Каталог брендов (02-catalog.html)
│       ├── Brand.jsx            # Страница бренда (03-brand.html)
│       ├── Lineup.jsx           # Страница линейки (04-lineup.html)
│       ├── Product.jsx          # Карточка товара (05-product.html)
│       ├── Cart.jsx             # Корзина (06-cart.html)
│       ├── Filters.jsx          # Фильтры fullscreen (07-filters.html)
│       ├── Profile.jsx          # Профиль (08-profile.html)
│       ├── Orders.jsx           # История заказов (09-orders.html)
│       ├── Favorites.jsx        # Избранное (10-favorites.html)
│       ├── QuickOrder.jsx       # Быстрый заказ (11-quick-order.html)
│       └── Settings.jsx         # Настройки (12-settings.html)
│
├── .env.local
├── package.json
├── vite.config.js
├── tailwind.config.js           # CommonJS! (module.exports)
├── postcss.config.js            # CommonJS! (module.exports)
└── vercel.json
```

---

## РОУТИНГ (App.jsx)

```
/onboarding     → Onboarding (если первый запуск)
/               → Home
/catalog        → Catalog (сетка брендов)
/brand/:id      → Brand (линейки бренда)
/lineup/:id     → Lineup (товары линейки)
/product/:id    → Product (карточка товара)
/cart           → Cart
/filters        → Filters
/quick-order    → QuickOrder
/orders         → Orders
/favorites      → Favorites
/profile        → Profile
/settings       → Settings
/auth           → Auth (логин/регистрация/восстановление)
```

### Auth Guard:
- Если клиент НЕ авторизован → показывать Onboarding
- Onboarding заканчивается регистрацией → сохранение сессии → редирект на /
- При каждом запуске: проверять `tts_auth_token` в localStorage → если есть, восстановить сессию
- Без авторизации нельзя: видеть каталог, добавлять в корзину, оформлять заказ

---

## SUPABASE — БАЗА ДАННЫХ

### Подключение:
```
URL: https://eaazhodmkmejfmbrmmwl.supabase.co
Anon Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVhYXpob2Rta21lamZtYnJtbXdsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA1NTAyNDMsImV4cCI6MjA4NjEyNjI0M30.n6V8d1NNu-I84p988EeOWkOs7t4KF5vgxhAtr_rWNKg
```

### Таблицы (ВСЕ ПОЛЯ НА РУССКОМ):

**товары** (561 запись):
- id, название, бренд, линейка, категория
- цена_безнал, цена_нал
- крепость, вкус, формат_пакетов, кол_во_пакетов
- фото_url (сейчас NULL у всех — показывать placeholder)
- активен, created_at

**клиенты** (29 колонок):
- id, telegram_id, telegram_username, имя, телефон
- дата_рождения, дата_регистрации, уникальный_номер
- баланс_ткоинов, сумма_всех_покупок, постоянная_скидка, количество_покупок
- статус, аватар, последняя_активность
- пароль_хеш, логин (UNIQUE), телефон_последние4
- любимая_марка, любимая_крепость, средний_чек
- реферальный_код, уровень_клиента, бросков_кубика
- согласие_на_обработку, согласие_на_уведомления
- активность_месяц, приглашённых_друзей

**заказы** (37 колонок):
- id, клиент_id, точка_id, тип_оплаты, статус
- итоговая_сумма, сумма_нал, сумма_безнал, выгода_за_нал
- скидка_активность, скидка_объём, скидка_кубик
- списано_ткоинов, начислено_ткоинов
- сдача_с, продавец_id, курьер_id
- комментарий, адрес_доставки, телефон_для_связи
- время_готовности, время_выдачи
- себестоимость_товаров, чистая_прибыль
- товары_json, предупреждение_документ, анонимный
- шайба_в_подарок, шайб_начислено
- created_at, updated_at

**точки** (9 записей, 3 активных):
- id, название, тип ("Магазин" с большой буквы!), адрес, активна, порядок
- Активные: ЦЕНТР (порядок 1), СЕВЕРНЫЙ (2), ЛБ (3)

**инвентарь:**
- id, товар_id, точка_id, количество

**смс_коды:**
- id, телефон, код, создан_в, истекает, использован

### RLS: SELECT открыт на все таблицы. INSERT/UPDATE открыт на заказы, клиенты, смс_коды.

---

## МАППИНГ ДАННЫХ

При получении данных из Supabase — маппить русские поля:
```javascript
// Товар
const product = {
  id: raw.id,
  name: raw.название,
  brand: raw.бренд,
  lineup: raw.линейка,
  category: raw.категория,
  priceCash: raw.цена_нал,
  priceCard: raw.цена_безнал,
  strength: raw.крепость,
  flavor: raw.вкус,
  format: raw.формат_пакетов,
  packets: raw.кол_во_пакетов,
  photo: raw.фото_url,
  active: raw.активен,
}
```

---

## БИЗНЕС-ЛОГИКА

### Иерархия каталога:
```
Бренд → Линейка → Товар (вкус)
Пример: FEDRS → Ice Cool → Mint Hard
```
Группировка делается на фронте из таблицы `товары` по полям `бренд` и `линейка`.

### Скидки за объём (при оформлении заказа):
| Кол-во | Скидка с каждой | Бонус |
|--------|----------------|-------|
| 2-4    | -30₽           | —     |
| 5-6    | -50₽           | +1 шайба в подарок |
| 7+     | -60₽           | +1 шайба в подарок |

### Постоянная скидка (по сумме_всех_покупок клиента):
| Сумма    | Скидка |
|----------|--------|
| 24 000₽  | 3%     |
| 44 000₽  | 5%     |
| 59 000₽  | 10%    |

### Ткоины:
- 1 ткоин = 1 рубль
- Кэшбэк: 1.5% от суммы (если не списываются ткоины)
- Максимум списания: 50% от суммы заказа
- При списании → кэшбэк не начисляется

### Оплата:
- Наличные (выгоднее: цена_нал < цена_безнал)
- Безналичные
- Смешанная: каждый товар отдельно помечается нал/безнал

### Наличие:
- Из таблицы `инвентарь` (JOIN товар_id + точка_id → количество)
- 🟢 5+ = "В наличии", 🟡 1-4 = "Мало", 🔴 0 = "Нет"

---

## ОПИСАНИЕ КАЖДОЙ СТРАНИЦЫ

### 1. Onboarding (13-onboarding.html)
- 4 горизонтальных слайда (swipe или кнопка "Далее")
- Слайд 1: анимированный логотип + 4 фичи
- Слайд 2: система скидок (3 порога)
- Слайд 3: ткоины (кэшбэк, кубик, рефералы)
- Слайд 4: регистрация (имя + дата рождения + чекбокс 18+)
- Кнопка НАЧАТЬ активна только после чекбокса
- После НАЧАТЬ → сохранить клиента в Supabase → авторизовать → redirect /

### 2. Home (01-home.html)
- Header: логотип TTS + кнопки (поиск, уведомления)
- Карточка профиля: аватар, имя, ID, скидка, ткоины
- Быстрые действия: 5 кнопок (каталог, быстрый заказ, история, избранное, предзаказ)
- Панель "ТОП-СПРОС СЕГОДНЯ": горизонтальный скролл, dots, стрелки
- Секция "ПОСЛЕДНИЕ НОВИНКИ": горизонтальный скролл
- 2 большие кнопки: "Выбрать самостоятельно" + "Выбрать с помощью ИИ"

### 3. Catalog (02-catalog.html)
- Поиск
- Быстрые кнопки: 🔥 Все, Табачный, Целлюлоза, Крепкие, Лёгкие, Новинки, Скидки
- Сетка брендов (карточки с логотипом, названием, кол-вом линеек/товаров, индикаторами наличия)
- Кнопки фильтров: Точка, Тип продукта, Крепость и т.д.
- Секция товаров: сетка 2 в ряд (ProductCard)
- Сортировка

### 4. Brand (03-brand.html)
- Hero: большой логотип бренда + название + описание
- Статистика: кол-во линеек, вкусов, диапазон цен
- Список линеек (карточки): иконка, название, описание, кол-во вкусов, крепость → стрелка
- "Популярное": горизонтальный скролл маленьких карточек
- Кнопка "Смотреть все N товаров"

### 5. Lineup (04-lineup.html)
- Header линейки с описанием
- Сетка товаров 2 в ряд
- У каждого: фото, название, характеристики в жёлтой рамке (крепость, паки), цена
- Индикаторы наличия по точкам (Ц/С/Л — цветные точки)
- Кнопка [+] добавить в корзину

### 6. Product (05-product.html)
- Большое фото товара с бейджами (ТОП ПРОДАЖ, NEW)
- Бренд > Линейка > Название
- Описание
- 6 характеристик в сетке 2x3: крепость, пакетиков, тип эффекта, формат, тип продукта, аромка
- Наличие по точкам (карточки с адресами и количеством)
- Шкалы характеристик: крепость, сила жжения, насыщенность аромки, общая оценка
- Отзывы: звёзды + текстовые отзывы
- Bottom bar: цена + qty selector + 2 кнопки (ПРЕДЗАКАЗ / ОФОРМИТЬ ЗАКАЗ)

### 7. Cart (06-cart.html)
- Список товаров с qty selector и переключателем нал/безнал (при смешанной оплате)
- Выбор точки получения (3 точки + доставка) с часами работы
- Предупреждения о наличии + кнопка ЗАМЕНИТЬ
- Способ оплаты: наличные (зелёный, выгодный), безнал, смешанный
- Поле списания ткоинов (ввод + кнопка МАКС)
- Итоговый блок: товары, скидка за объём, выгода за нал, списание ткоинов, ИТОГО
- Предупреждение о паспорте 18+
- Кнопка ОФОРМИТЬ ЗАКАЗ

### 8. Filters (07-filters.html)
- Fullscreen page (не модалка!)
- Секции: Точка (с цветными дотами), Тип продукта, Крепость (шкалы!), Тип эффекта, Пакетики, Кол-во паков, Аромка
- Chips с toggle active
- Крепость: 9 уровней с progress bars
- Bottom bar: СБРОСИТЬ + ПОКАЗАТЬ (N)

### 9. Profile (08-profile.html)
- Карточка: аватар, имя, ID, дата рождения, заказы, выкуп, бейдж скидки
- Панель ткоинов
- Любимая позиция: премиум-карточка с золотой рамкой-градиентом
- Прогресс постоянной скидки (3 порога с progress bar)
- Активность за месяц: 5 заданий (2x2 сетка + карточки)
- Награда: "100% скидка на шайбу" (анимированная панель)
- Меню: история заказов, избранное, пригласить друга, поддержка, правила

### 10. Orders (09-orders.html)
- Статистика: всего заказов, сумма выкупа, ткоины
- Фильтры: ВСЕ, АКТИВНЫЕ, ЗАВЕРШЁННЫЕ, ОТМЕНЁННЫЕ
- Карточки заказов: номер, дата, статус (цветной бейдж), список товаров, точка, итого
- Кнопки по статусам: ПОВТОРИТЬ, ОТМЕНИТЬ, ОТЗЫВ и т.д.

### 11. Favorites (10-favorites.html)
- Баннер уведомлений (toggle)
- Сетка 2 в ряд
- Красное сердце (при клике — удаление)
- Бейджи: ТОП, NEW, -15%
- Индикаторы наличия (Ц/С/Л)
- Товары без наличия: затемнение + плашка "НЕТ В НАЛИЧИИ"

### 12. Quick Order (11-quick-order.html)
- Hero: молния + "ЗАКАЗ В 2 КЛИКА"
- 2 карточки: "Повторить последний" + "Из избранного"
- Выбор точки (чипы с индикаторами)
- Последние заказы с кнопкой ПОВТОРИТЬ
- "Часто заказываешь": горизонтальный скролл карточек
- Секция "НЕТ НА ТОЧКЕ" с аналогами

### 13. Settings (12-settings.html)
- Профиль: аватар, имя, телефон, поля ввода
- Уведомления: toggles (push, избранное, закрытие, акции)
- Предпочтения: точка по умолчанию, способ оплаты
- Поддержка: написать, правила, FAQ
- Danger zone: выйти, удалить аккаунт

---

## ПОРЯДОК РЕАЛИЗАЦИИ

### Фаза 1 — Скелет (день 1):
1. Настроить Tailwind с кастомными CSS-переменными
2. Создать AppShell, BottomNav, Header
3. Роутинг всех страниц
4. Zustand stores (auth, cart, filters)
5. Supabase client

### Фаза 2 — Онбординг + Авторизация (день 1-2):
1. Onboarding (4 слайда)
2. Auth guard в App.jsx
3. Регистрация → Supabase (таблица клиенты)
4. Автологин из localStorage

### Фаза 3 — Каталог (день 2-3):
1. Catalog → Brand → Lineup → Product (иерархия)
2. Компоненты: ProductCard, ProductCardSmall, StockDots
3. Фильтры (fullscreen)
4. Поиск

### Фаза 4 — Корзина + Заказ (день 3-4):
1. Cart store (Zustand)
2. Cart page: выбор точки, оплата, ткоины
3. Расчёт скидок (объём, постоянная, ткоины)
4. API create-order (атомарная транзакция)

### Фаза 5 — Остальные страницы (день 4-5):
1. Home (с данными из Supabase)
2. Profile
3. Orders
4. Favorites
5. Quick Order
6. Settings

---

## ВАЖНЫЕ НЮАНСЫ

1. **postcss.config.js и tailwind.config.js** — ТОЛЬКО CommonJS (`module.exports = {}`)
2. **Vercel Serverless Functions** — папка /api, формат ESM (import/export)
3. **Supabase URL** — начинается с `ea` (eaazhodmkmejfmbrmmwl)
4. **Точки** — тип = "Магазин" (с большой буквы, не "МАГАЗИН")
5. **Фото товаров** — все `фото_url = NULL`, показывать emoji-placeholder
6. **Пустые строки в .env.local** ломают Vite парсинг — не оставлять
7. **Git remote** содержит token — не показывать
8. **Кожаная текстура** — нужен файл `public/assets/leather-texture.png` (если нет — создать CSS fallback: `background: repeating-linear-gradient(...)` тёмно-коричневого цвета)

---

## ENV VARS (.env.local)

```
VITE_SUPABASE_URL=https://eaazhodmkmejfmbrmmwl.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVhYXpob2Rta21lamZtYnJtbXdsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA1NTAyNDMsImV4cCI6MjA4NjEyNjI0M30.n6V8d1NNu-I84p988EeOWkOs7t4KF5vgxhAtr_rWNKg
```

Vercel env vars (для серверных функций):
```
SMSRU_API_ID=D62EC9E9-1EC0-83D4-26F4-21F6A8B99454
```

---

## КАК НАЧАТЬ

```bash
cd ~/Downloads/tts-client-app
# Прочитай этот файл
# Прочитай HTML-макеты в папке /mackets (или как они будут названы)
# Удали старые файлы из src/pages и src/components (кроме lib/supabase.js)
# Начни с Фазы 1: AppShell + BottomNav + роутинг
# Запусти npm run dev и проверяй визуально после каждого шага
```
